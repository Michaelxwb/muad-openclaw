# 用户密钥/运行态，绝不入库
users/*/
!users/_template.config

# 杂项
node_modules/
*.log
.DS_Store
.idea/

# 运行态 SQLite DB（控制台），绝不入库
console/backend/data/

# console 运行时配置（含 masterKey/adminPassword），绝不入库；模板见 config.example.yaml
console/config.yaml
console/backend/config.yaml
# 旧 env 部署方式已弃用（如有遗留也不入库）
console/.env
