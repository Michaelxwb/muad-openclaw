# code-flow 运行时数据（仅存本地，不入库）
.session-log.jsonl
.check-state.json
# >>> code-flow:runtime schema=1
.active-task.json
.active-task.lock
.catalog-state.json
migrations/
specs/_session/
# <<< code-flow:runtime schema=1
