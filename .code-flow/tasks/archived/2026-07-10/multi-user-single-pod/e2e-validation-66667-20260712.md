# 66667 Multi-User E2E Validation (2026-07-12)

## Final Artifact And State

- Image: `muad-openclaw:multi-user-20260712.5`
- Image digest: `sha256:b51485cf79a493e9a5eab446425e7ceb69d3021cde6d934a6573ffd65a039b25`
- Architecture/OpenClaw: `arm64`, `2026.6.10`
- Final Pod state: `running`, desired/applied generation `34/34`
- Final Runtime health: `ok=true`, three user mappings, Skill limit 2, Browser limit 2
- Service token: `0400`, owner `1000:1000`, mounted from a file rather than environment variables

The development database was rebuilt from the new schema. Pod `66667`, its channel configuration,
Alice/Bob/Charlie, four real identities, and the existing per-user model settings were restored through
the new API contract. The legacy database remains in the dated local backup files documented by the task log.

## Restart And Generation Safety

The retained PVC exposed a startup race: the Deployment environment still contained generation 1 while
the PVC contained a newer committed runtime configuration. `inject-env.mjs` now rejects stale startup DTOs.
After a real Pod deletion, startup reported `generation=8 source=persisted`; the final image repeated the
test with persisted generation 14 and then reconciled normally to generation 15. Agents, routes, browser
profiles, identity links, and all three Muad plugins survived both restarts.

## Binding And Cross-IM Identity

- Created a scoped, one-time WeChat binding code for existing user Bob.
- Activated it with the Pod service token and a direct-message identity; the code became `used` with zero
  failed attempts and generation 13 rendered Bob's WeCom and WeChat routes plus shared identity links.
- Removed the synthetic identity after inspection; generation 14 restored the real route set. The used code
  remains as non-secret audit evidence, while its plaintext was never logged or persisted.
- The user previously confirmed that Alice's WeCom and WeChat identities share memory while sessions remain
  channel-scoped. Runtime config retains both identities under one Alice identity-link entry.

## Concurrent Models

Alice and Charlie were invoked concurrently with independent sessions. Both calls succeeded without fallback:

| Agent | Expected provider | Observed provider | Result | Duration |
| --- | --- | --- | --- | --- |
| Alice | `user-alice-deepseek` | `user-alice-deepseek` | `ALICE_OK` | 2314 ms |
| Charlie | `user-charlie-deepseek` | `user-charlie-deepseek` | `CHARLIE_OK` | 2361 ms |

The rendered provider key fingerprints differ. Bob continues to inherit `pod-default-deepseek`.

## Concurrent Browser Isolation

Alice and Bob each performed a real Browser Tool call concurrently:

| Agent | Profile/CDP | Observed URL | Result |
| --- | --- | --- | --- |
| Alice | `alice` / `18802` | `https://example.com/?muad=alice` | one successful Browser call |
| Bob | `bob` / `18803` | `https://example.org/?muad=bob` | one successful Browser call |

The quarantine profile remained empty. A second Alice run explicitly requested `profile=bob`; Runtime Guard
rejected it as cross-profile access, recorded one failed tool call, and Bob's tab remained unchanged.

## Platform Credential And Session Cache

A temporary in-cluster platform service and a random Alice XDR key exercised the complete Resolver and
session-manager path. The temporary service derived a non-secret session value from the key fingerprint and
never logged authorization headers.

1. First stable resolve returned `source=refresh`; the next cross-IM session returned `source=cache`.
2. Credential rotation changed the fingerprint and forced `source=refresh` on the next resolve.
3. Platform disable returned `platform_disabled` and removed all three cached state files.
4. Re-enable refreshed state; credential deletion returned `not_configured` and removed the cache again.
5. `cookies.json`, `storageState.json`, and `meta.json` were `0600`, owned by `1000:1000`, matched Human User,
   Agent, Pod, credential, and platform fingerprints, and did not contain the API key.

A transient adapter transport failure found during setup was incorrectly non-retryable. The HTTP adapter now
normalizes transport failures to a redacted retryable error; 20 session-manager tests pass. The random key,
mock Deployment/Service, cached state, and temporary XDR configuration were removed after validation.

## Final Payload Delivery

- Existing manual WeCom and WeChat progress tests remain valid; no upstream OpenClaw or channel plugin code
  is modified. The accepted WeCom ordering behavior is unchanged.
- A real WeCom core send delivered final text plus an Alice-profile browser PNG. OpenClaw returned message ID
  `aibot_send_msg_1783833753983_09aa226c` with both `mediaUrl` and `mediaUrls` in the final payload.
- Progress handling is confined to `muad-run-skill`; final text/media delivery remains owned by OpenClaw core.
