# Hermes Progress Adapter

This adapter converts `muad-progress` event dictionaries into Hermes plugin/tool progress.

It is a thin adapter only. Business skills still call `/usr/local/bin/muad-progress`; platform login state remains owned by session-manager.
